const nTerm = NodeSpace.term;

export const redBckLogger = nTerm.buildLogger(nTerm.B_RED);
export const greenLogger = nTerm.buildLogger(nTerm.C_GREEN);
export const blueLogger = nTerm.buildLogger(nTerm.C_BLUE);